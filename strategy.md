# HOTICE Testing Strategy

The Phase 0 end-to-end suite validates the visible foundation shell and its backend health contract. Later phases will expand the suite as user-visible capabilities are introduced.

The tests intentionally do not exercise authentication, messaging, Marketplace, Status, calls, or other deferred features because those features do not exist in Phase 0.